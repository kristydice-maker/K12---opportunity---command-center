name: Build E-Rate Account Intelligence Brief

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - "collectors/build_erate_brief.py"
      - "collectors/build_erate_pdf.py"
      - "data/usac_470_activity.json"
      - "data/usac_471_activity.json"
      - "data/usac_frn_line_items.json"
      - ".github/workflows/build-erate-brief.yml"
  schedule:
    - cron: "0 13 * * *"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PDF dependency
        run: python -m pip install reportlab

      - name: Build consolidated E-Rate brief
        run: |
          python collectors/build_erate_brief.py
          python collectors/build_erate_pdf.py

      - name: Commit consolidated brief
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/erate_account_intelligence.md data/erate_account_intelligence.pdf
          git diff --cached --quiet || git commit -m "Update consolidated E-Rate intelligence brief"
          git pull --rebase
          git push
